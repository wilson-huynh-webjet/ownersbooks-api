const books = require("../data/sampleData");

const getBooks = () => {
  // TODO: remove this sampleData and implement real data source. This sampleData is for demo purposes only
  return books;
};

module.exports = getBooks;
